/**
 * You can run this code by using the following command
 * $ node index.js
 * $ bun index.js
 * $ npm run start
 * $ npm run charandeep
 */

// function to add two numners and return the sum
function sum(a, b) {
    return a, b;
}

// calling the function and storing the result in a variable ans
const ans = sum(10, 20);

// printing the result to the console
console.log(ans);