/** 
 * Use `node fileName.js` command to run this file
 * Can also use `bun fileName.js` to run this file
 */

// Each language have concept of importing/bringing external libraries

// node.js => pakage.json --> configration file of the project
// rust => cargo (package manager for Rust)

// deploying an external package to npm registry

// function to add two numners and return the sum
function sum(a, b) {
    return a, b;
}

// calling the function and storing the result in a variable ans
const ans = sum(10, 20);

// printing the result to the console
console.log(ans);